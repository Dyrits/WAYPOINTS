# Agent notes

- Handoff documents live in `.agents/handoffs/` and are committed with the repository (decided 2026-09-24).

## Agent skills

### Issue tracker

Markdown local sous `backlog/<feature-slug>/`, committé. Les brouillons se font dans `.refinement/` et ne passent dans `backlog/` que sur demande explicite de publication. See `documentation/agents/issue-tracker.md`.

### Triage roles

Libellés par défaut : catégories `bug`, `enhancement`, états `to-evaluate`, `on-hold`, `ready`, `not-planned`. See `documentation/agents/triage-roles.md`.

### Domain documentation

Single-context : `CONTEXT.md` et `documentation/architecture-decision-record/` à la racine. See `documentation/agents/domain.md`.
