# Handoff: V3 split into 13 draft tickets, ready to implement

Supersedes: `.agents/handoffs/2026-09-25-0115-v3-spec-and-reorg.md`. Its layout, harness notes and delivery order still hold; its open points 1 to 4 are settled below.

## State

Branch `V2`, last commit `d88d08c`, in sync with `origin/V2`. This session's changes are **uncommitted**:

- `.refinement/v3-carnet-de-route/specification.md`: now written entirely in English (domain terms and chapitre titles stay French). Decisions below recorded under Further Notes.
- `.refinement/v3-carnet-de-route/issues/01` to `13`: draft tickets (`Status: draft`, built-in `to-tickets` template), each with a `Blocked by` line and acceptance criteria. Not published to `backlog/`.
- `CONTEXT.md`: the Sac à dos definition now says the marcheur carries it from the first escale, in every tenue.

The user was offered a commit and has not answered. Commit before starting work, or ask.

## Decisions settled with the user this session (2026-09-25)

- Sac à dos visible from the start in every tenue; the Visa ~~Vacances~~ Travail tenue is the hat alone.
- Annexes excluded from escale numbering; header shows "Interlude" at Bouguenais.
- Day and night from time zones stays out of scope (already in the spec).
- Working language: the user left it to the agent. Spec and tickets are in English; French domain terms from `CONTEXT.md` are kept verbatim. Site copy stays French.
- Granularity and blocking edges: the user delegated ("fais ce qui est le mieux"). The double page is split into prototype (04) and integration (05). Apprenants (08), compagnon IA (09) and sac à dos (10) are blocked by the saut (02) so their "no saut interaction" criteria are verifiable.

## Frontier

01 (harness) and 04 (double page prototype) have no unresolved blockers. 01 first, since every other ticket relies on it for checks and screenshots. 04 is independent and goes on a `prototype/double-page` branch; its outcome is written back into ticket 05. Full graph: the `Blocked by` line of each ticket file.

## Things to know

- Harness is still in `/tmp/shot` (see the superseded handoff, section "Verification harness"); ticket 01 moves it. `/tmp` may have been cleaned: if so, rebuild from scratch with puppeteer-core and the system Chrome.
- The `to-tickets` skill has `disable-model-invocation`; the same likely applies to other workflow skills. If the Skill tool refuses one, read `~/.claude/skills/<name>/SKILL.md` and follow it.
- Chapitre opening sentences, the annexe sentence and skills tags must be drafted by the agent and validated by Dylan before merge (criteria in tickets 03, 10, 12).

## Suggested skills

- `implement` (or `test-driven-development`): ticket 01, then each frontier ticket, `/clear` between tickets.
- `prototype`: ticket 04.
- `run`: screenshots of `V3/` and `V3/lab.html` after each visual change.
- `unslop`: chapitre sentences and annexe copy.
- `domain-modeling`: if a new term appears.
- `code-review`: closes each ticket; `to-pull-request` when merging `V2` into `main`.
