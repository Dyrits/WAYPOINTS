# Handoff: V3 grilled and specified, repository split into V1 / V2 / V3

Supersedes: `.agents/handoffs/2026-09-25-0018-v2-stickman-popup.md`. The stickman, pop-up and Lyon work it describes was committed in `cf1bc47`, despite the handoff saying it was uncommitted. Its "Agreed V3 plan" is replaced by the specification below.

## State

Branch `V2`. This session's work is committed and pushed in the commit following `cf1bc47`. Primary sources:

- `.refinement/v3-carnet-de-route/specification.md`: V3 draft specification (`Status: draft`, not published to `backlog/`). Chapters table, 54 user stories, implementation and testing decisions, out of scope, open points.
- `CONTEXT.md`: domain glossary (Escale, Chapitre, Interlude, Annexe, Lieu, Marcheur, Tenue, Apprenant, Compagnon IA, Saut, Fil rouge, Sac à dos). Use these terms.
- `documentation/agents/`: skill config set up this session. Local markdown tracker (`backlog/`), built-in `to-tickets` format, default triage roles, single-context domain docs. `AGENTS.md` has the `## Agent skills` block.
- `README.md`: new layout.

## Repository layout (changed this session)

`V1/` holds the globe site, including `land.js`. `V2/` is the frozen reference (formerly `v2/`). `V3/` starts as a copy of `V2/` and is where V3 gets built. `shared/data.js` holds the content, loaded by all three. The root `index.html` redirects to `V1/` until V3 goes live. `tools/build-land.mjs` writes to `V1/land.js`. Folder names use an uppercase V. The user asked for uppercase in V3 references too.

The user did not explicitly answer the layout questions (shared content folder, V3 seeded from V2, root redirect, where `land.js`/`tools`/`archive` go). They only corrected the casing, so the recommendations were applied. Easy to revisit.

## Open points awaiting the user

1. The Visa ~~Vacances~~ Travail outfit includes a backpack, while the Sac à dos inventory implies the walker always carries one. Is it visible from the start, or does it appear in chapter 3 and stay?
2. The annexe is excluded from the escale numbering and the header shows "Interlude" for Bouguenais. The agent derived both; the user has not confirmed them.
3. Q20: day and night from the time-zone gap was marked abandoned in favour of per-chapter light. The user never answered it explicitly.
4. The specification's user stories are in English (skill template), the rest in French. The agent offered to translate; no answer yet.
5. The Part-Dieu towers still need a faithful redraw (Crayon, Incity or Oxygène), from the previous handoff.

## Verification harness

It is still in `/tmp/shot` (puppeteer-core, system Chrome). Scripts `shot2.mjs`, `burst.mjs`, `lab.mjs`, `walk.mjs` hardcode the old `v2/` path and must point to `V3/`. `check.mjs` loads every page and reports errors; it passed after the reorganisation. Moving the harness to `tools/shot/` with a dev-only `package.json` is a spec decision and belongs to a ticket. Rail clicks drive navigation headless; keyboard presses do not reach the page. Always `node --check` the scripts.

## Next

Run `/to-tickets` on the specification to draft tracer-bullet tickets under `.refinement/v3-carnet-de-route/issues/`, with blocking edges. Delivery order: jump, chapters (double page and light), outfits and followers, backpack, red thread, interlude and annexe, ending. The harness move comes first or alongside the jump. Settle the open points 1 to 3 with the user before the tickets that depend on them. Publish to `backlog/` only when asked.

## Suggested skills

- `to-tickets`: next step, from the specification.
- `prototype`: chapter boundary and double page before integrating.
- `implement` / `test-driven-development`: per ticket, starting with the jump.
- `run`: screenshot `V3/` and `V3/lab.html` after each visual change.
- `unslop`: chapter opening sentences and narration copy.
- `domain-modeling`: when a new term appears or an open point changes a definition.
- `code-review` or `simplify`: before merging `V2` into `main`.
